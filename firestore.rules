rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /professionals/{profId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == profId;
      
      match /services/{serviceId} {
        allow read: if true;
        allow write: if request.auth != null && request.auth.uid == profId;
      }
      
      match /products/{productId} {
        allow read: if true;
        allow write: if request.auth != null && request.auth.uid == profId;
      }
      
      match /leads/{leadId} {
        allow create: if true;
        allow read, update, delete: if request.auth != null && request.auth.uid == profId;
      }
      
      match /appointments/{apptId} {
        allow read, write: if request.auth != null && request.auth.uid == profId;
      }

      match /expenses/{expenseId} {
        allow read, write: if request.auth != null && request.auth.uid == profId;
      }

      match /estimates/{estimateId} {
        allow read, write: if request.auth != null && request.auth.uid == profId;
      }

      match /receipts/{receiptId} {
        allow read, write: if request.auth != null && request.auth.uid == profId;
      }

      match /reviews/{reviewId} {
        allow read: if true;
        allow create: if true;
        allow update, delete: if request.auth != null && request.auth.uid == profId;
      }
    }
  }
}
